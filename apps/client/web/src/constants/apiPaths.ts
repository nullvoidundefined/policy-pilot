/**
 * Backend API route paths shared across api wrappers and pages, kept in one place
 * so the CSRF and Q&A-stream endpoints are referenced by name rather than by literal.
 */
export const CSRF_TOKEN_PATH = '/api/csrf-token';
export const QA_STREAM_PATH = '/qa';
